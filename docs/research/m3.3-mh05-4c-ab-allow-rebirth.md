# M3.3 MH_05 A/B：`--allow-culled-rebirth`（只关 reject-set）

日期：2026-08-02  
状态：**完成** — 关 `block_culled_rebirth` **无效**；轨迹与默认对照
bit-identical。主因指向 session `dropTracks`。  
相关：

- 失败诊断 [`m3.3-mh05-failure-diagnosis.md`](m3.3-mh05-failure-diagnosis.md)
- 诊断设计 [`m3.3-mh05-failure-diagnosis-design.md`](m3.3-mh05-failure-diagnosis-design.md)
- ④c 设计 [`m3.3-slice4c-cull-track-drop-design.md`](m3.3-slice4c-cull-track-drop-design.md)
- issue [#24](https://github.com/Nothand0212/phad-vio/issues/24)

本文档描述当前约定，不是绝对约束，会随项目开发修订。

## 1. 臂与身份

| 臂 | commit | label / hash | `block_culled_rebirth` | session dropTracks |
|---|---|---|---|---|
| 对照 | `0ced28b` | `default` / `3a21162e` | true | 开（无开关） |
| 实验 | `34d4905`（含 ④e 默认） | `allow_rebirth` / `be30fbc9` | **false** | 开（仍强制） |
| ④b 参考 | `3855395` | `default` / `65273570` | （无键） | **无** drop |

命令：

```bash
./build/phad_vo_bench \
  /home/lin/Projects/data/thidparty/euroc/native/MH_05_difficult \
  --bench-root /home/lin/Projects/data/phad-bench \
  --gt-euroc /home/lin/Projects/data/thidparty/euroc/native/MH_05_difficult \
  --allow-culled-rebirth \
  --config-label allow_rebirth
```

产物：`…/MH_05_difficult/34d4905/allow_rebirth_be30fbc9/`

## 2. 数字

| 项 | 对照 `3a21162e` | 实验 `be30fbc9` | ④b |
|---|---:|---:|---:|
| ATE trans RMSE (m) | **4.565065** | **4.565065** | 3.448773 |
| RPE trans RMSE (m) | 1.250271 | 1.250271 | 0.919759 |
| outliers_culled / unique | 28 / 28 | 28 / 28 | 72 / 66 |
| outlier_reopts | 1 | 1 | 2 |
| completion | 0.9978 | 0.9978 | 0.998 |

`est.tum`：实验 vs 对照 **2268 帧位姿全部 bit-identical**（关 reject-set 在
本序列上零可观测差异）。

## 3. i=425–436 时间线

实验与对照 **同型**；均未恢复 ④b 二次大剔：

| i | 对照/实验 culled | ④b culled | 实验 reproj_after | ④b reproj_after |
|---:|---:|---:|---:|---:|
| 425 | 24 | 24 | 297→0.18 | 297→0.18 |
| 430 | 0 | 0 | **9.61** | **0.27** |
| 436 | **0** | **44** | **5.83** | 204→0.26 |

## 4. 判定（按对齐出口 A）

| 检查 | 结果 |
|---|---|
| 再现 ④b 式二次 `culled≥4`+reopt | **否** |
| ATE 相对 4.565 明显下降 | **否**（持平 / tum 相同） |
| 分支 | **升级 dropTracks 对照** |

**结论**：在现行「culled 后必 `dropTracks`」下，单独关闭
`block_culled_rebirth` **不能**解释/修复 MH_05 相对 ④b 的回归。双保险里
对 MH_05 分叉起决定作用的是 **`dropTracks`（或 drop∧reject 中 drop 主导）**。

## 5. 下一刀（归因续）

需要显式 A/B：**大剔后是否调用 `dropTracks`**（当前无 CLI；④c 设计曾
YAGNI）。建议最小改动：

- session / bench 增加诊断用开关（如 `--no-drop-culled-tracks`），进
  `config_hash`；
- MH_05：默认 drop vs 不 drop（`block_culled_rebirth` 可保持 true 或再析因）；
- 判定仍用时间线门 + 软 ATE；MH_01 在出现「有效改善」的配置后再跑不回归。

**不**在本步改默认生产行为；先拿数字再开 ④f 机制设计。
