# M3.3 子集延后 drop 探针（`--defer-drop-topk`）

日期：2026-08-02  
状态：**探针矩阵已跑** — 默认 k=0（④f）；不进 `config_hash`。  
矩阵结论：非零 k 均劣于 ④f；k=32≡④e；**无插值甜区**（按 id 排序取前 k）。  
相关：

- 候选 [`m3.3-post4g-next-knife-candidates.md`](m3.3-post4g-next-knife-candidates.md)
- ④g 事后 [`m3.3-slice4g-postmortem.md`](m3.3-slice4g-postmortem.md)
- issue [#24](https://github.com/Nothand0212/phad-vio/issues/24)

本文档描述当前约定，不是绝对约束，会随项目开发修订。

## 行为

| 项 | 约定 |
|---|---|
| CLI | `--defer-drop-topk <k>`，`k≥0`，默认 **0** |
| 选取 | skip 当帧：`culled_landmark_ids` 按 id **升序**取前 k |
| 时机 | 下帧 `tracker.process` 前 flush（同 ④g） |
| hash | **不**进 `flattenConfig` |
| 计数 | flush 累加 `deferred_drops` / `deferred_drop_ids` |
| k=0 | ④f（skip 且不排队） |
| k≥\|culled\| | 等价完整延后 drop（MH_05 上期望 ≈④e） |

## 矩阵验收（MH_05，commit `2e5a06e`，scratch `/tmp/mh05_topk`）

| k | ATE | RPE | deferred_drops / ids | cheirality | 备注 |
|---:|---:|---:|---:|---:|---|
| 0 | **3.056878** | 0.772423 | 0 / 0 | 29 | ≡④f `est.tum` bit-identical |
| 4 | 4.030601 | 1.049456 | 1 / 4 | 26 | 劣于 ④f |
| 8 | 4.002654 | 1.042360 | 1 / 8 | 26 | 劣于 ④f |
| 16 | **4.812893** | 1.291522 | 1 / 16 | 29 | 劣于 ④e |
| 32 | **4.565065** | 1.250271 | 1 / 32 | 25 | ≡④e `est.tum` bit-identical |

`config_hash` 全程 `a5e90dc7`（探针不进 flatten）。  
i=426 `zombie_track_n`：k=0→32；k=4→28；k=8→24；k=16→16；k=32→0。

**解读**：按 `LandmarkId` 排序的子集延后 drop **没有**落在 3.06–4.56 之间的可用甜区；
任意测到的非零 k 都毁 ④f ATE。下一刀应转向候选 B/C，或换子集定义（非「id 序前 k」）
再探针——**不**把本旋钮升为默认。
