# M3.3 MH_05 失败模式诊断设计（④e 后续 · 先诊断再选刀）

日期：2026-08-02  
状态：**已执行** — 诊断笔记
[`m3.3-mh05-failure-diagnosis.md`](m3.3-mh05-failure-diagnosis.md) 已写出；
结论：主因是 ④c 大剔后路径，不是 ④e/Huber。  
相关：

- roadmap [M3.3](../roadmap.md)
- ④e 设计 [`m3.3-slice4e-multiround-reopt-design.md`](m3.3-slice4e-multiround-reopt-design.md)
- 基线 [`m3.3-slice4-baseline.md`](m3.3-slice4-baseline.md) §6（④b）/ §10（④e）
- issue [#24](https://github.com/Nothand0212/phad-vio/issues/24)

本文档描述当前约定，不是绝对约束，会随项目开发修订。

## 1. 目标与非目标

**唯一目标**：对 MH_05 做一次离线对照诊断，解释 ④b（ATE ≈ **3.45** m）→
④e（ATE ≈ **4.565** m）变差主因，并在诊断笔记末给出**唯一推荐下一刀**与
否决项，供后续 ④f 对齐使用。

后续切片节奏（已对齐）：**先诊断，再按诊断优化**；不为空转扫参。

不做：

- 改 `phad/` / `apps/` / `diag.csv` 合同 / 默认配置；
- 入库长期 `scripts/` CLI（允许会话内一次性 Python，用完可丢）；
- 跑全序列、开 Slice ⑤、改阈值蒙混过门；
- 在诊断完成前预定「去 Huber / 观测级」为结论。

## 2. 已对齐的决策

| 决策点 | 结论 |
|---|---|
| 节奏 | 先诊断 → 再选刀（续片同） |
| 深度 | 只吃现有产物（选项 A）；不够再升级探针 B |
| 对照 | MH_05：④e vs ④b（不做本轮发散序列抽检） |
| 方法 | 一次性 Python + 已有 `phad_traj_eval --errors-csv`（做法 1） |
| 出口 | 笔记必须给出唯一推荐下一刀 + 否决；或明确 B 升级缺口 |
| 图表 | 本轮不强制附带图；归因卡住再补 |
| 票务 | 续 [#24](https://github.com/Nothand0212/phad-vio/issues/24)；本片称 **MH_05 诊断**（④f 之前） |

### 输入（只读）

| 标签 | 路径 |
|---|---|
| ④b | `/home/lin/Projects/data/phad-bench/MH_05_difficult/3855395/default_65273570/`（`diag.csv` / `est.tum` / `summary.json` / `meta.json`） |
| ④e | `/home/lin/Projects/data/phad-bench/MH_05_difficult/0ced28b/default_3a21162e/`（同上） |
| GT | EuRoC ASL native：`…/euroc/native/MH_05_difficult`（经 `--gt-euroc`） |

### 产出

`docs/research/m3.3-mh05-failure-diagnosis.md`（无日期前缀）。

## 3. 数据流与步骤

```text
④b/④e est.tum ── phad_traj_eval -a --gt-euroc <seq> --errors-csv ──► errors.csv
④b/④e diag.csv ── join on timestamp_ns ────────────────────────────────┤
④b/④e summary/meta ── 身份与汇总 ──────────────────────────────────────┤
                                                                        ▼
                                                         一次性 Python 对照
                                                                        ▼
                                          docs/research/m3.3-mh05-failure-diagnosis.md
```

1. **身份核对**：两边 `meta.json` 的 commit / `config_hash` / 相对增量键；
   确认 summary ATE 与基线一致（≈3.45 vs ≈4.565）。
2. **逐位姿误差**：对两边 `est.tum` 跑
   `phad_traj_eval --est … --gt-euroc <MH_05> --max-dt-ms 2.5 --errors-csv …`
   （与 bench 同 SE3 ATE 语义）；**不**手写对齐。
3. **时间线对照**：`errors.csv` ⋈ `diag.csv`，比较平移误差分叉、
   `reproj_rms_after(_cull)`、`outliers_culled`、`num_cheirality`、
   `pnp_success` / inliers、`segment_id`、`low_connectivity`；关注
   「高误差但 culled≈0」与「大剔后仍高 rms」。
4. **事件表**：分叉点前后与 cull 峰值帧簇，用数字支撑假说。
5. **收敛**：笔记末唯一推荐刀 + 否决；不足则写清升级 B 的缺口。

临时文件放 `/tmp` 或未跟踪目录；不提交 `errors.csv` / 一次性脚本
（除非事后要求入库）。

若 `phad_traj_eval` 未在 `build/`，先
`cmake --build build --target phad_traj_eval` 再跑。

## 4. 诊断笔记格式

1. 元信息：日期、状态、对照身份、关联文档 / #24  
2. 汇总表：ATE/RPE、culled、reopt、cheirality、pnp、seg/re/failed  
3. 分叉时间线与关键帧簇表  
4. 假说检验（Huber 遮罩 / 观测级毒点 / mean-cull 宽严 / ④c 拒复生副作用 /
   其它）：每条 **支持 / 反对 / 不足**  
5. **结论（必填）**：唯一推荐下一刀；否决项；或 B 升级清单  

### 缺口处理

- GT / `est.tum` 对不齐或 match rate 异常 → 先修关联，不硬猜。  
- 仅靠 18 列 diag 无法区分假说 → 结论写「升级 B」，不假装选定机制。

## 5. 验收

| 项 | 标准 |
|---|---|
| 只读 | 无 `phad/` / `apps/` 合同改动；无正式 scripts 入库（除非另请） |
| 可复现 | 注明命令与路径；summary ATE 与基线一致 |
| 出口完整 | 有唯一推荐刀 + 否决，或明确的 B 升级清单 |
| 下游 | 笔记可直接作为 ④f 对齐输入 |

本片**不**另写编码实施计划；诊断笔记通过审阅后，再开 ④f 机制对齐
（模块边界 → 数据流 → 错误与诊断 → 测试与验收）。

## 6. 与候选刀的关系（诊断前不预定）

| 候选 | 本片态度 |
|---|---|
| 末轮去 Huber | 假说之一，待证据 |
| 观测级可翻转外点 | 假说之一，待证据 |
| 改 mean 判据（如 max）/ 探针 B | 若 A 不足则升级 |
| 再拧 `max_outlier_reopts` | **预否决倾向**（④e 已证伪多轮未动用），除非诊断反证 |
| Slice ⑤ 关键帧 | 本阶段不开（与 M4 耦合；MH_05 债未决） |
