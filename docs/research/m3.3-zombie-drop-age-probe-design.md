# M3.3 多帧 zombie 龄 drop（`--zombie-drop-age` / `session.zombie_drop_age`）

日期：2026-08-03  
状态：**已产品化默认 n=5** — 进 `flattenConfig` / `config_hash`；CLI 可覆写；
`0`=关（④f 行为）。  
相关：

- 候选 [`m3.3-post4g-next-knife-candidates.md`](m3.3-post4g-next-knife-candidates.md) §3 候选 B
- 完整集懒腾槽否决 [`m3.3-evict-skip-culled-probe-design.md`](m3.3-evict-skip-culled-probe-design.md)
- id 序 top-K 否决 [`m3.3-defer-drop-topk-probe-design.md`](m3.3-defer-drop-topk-probe-design.md)
- issue [#24](https://github.com/Nothand0212/phad-vio/issues/24)

本文档描述当前约定，不是绝对约束，会随项目开发修订。

## 行为

| 项 | 约定 |
|---|---|
| 默认 | `session.zombie_drop_age = **5**` |
| CLI | `--zombie-drop-age <n>`，`n≥0`，覆写默认 |
| 状态 | `OfflineVoSession`：`id → 连续存活帧龄`；frontend 无龄语义 |
| 入表 | 仅 **skip** 当帧的 `culled_landmark_ids`；已在表则保留原龄（`try_emplace` 龄=1） |
| 计龄 | 每帧 `tracker.process` 后、`update` 前：仍在 `FrameTracks` 则 +1，否则删项 |
| drop | `update` / skip 入表之后：龄 `≥n` 的 id 精确 `dropTracks`，并从表删除 |
| hash | **进** `flattenConfig`（`session.zombie_drop_age`） |
| 计数 | `zombie_age_drops`（冲刷次数）、`zombie_age_drop_ids`（累计 id 数） |
| n=0 | 关（④f：skip 且不按龄 drop） |

## 门控

| 臂 | 结果 |
|---|---|
| MH_05 探针 `n=0` | ATE ≡④f ≈ **3.056878** |
| MH_05 探针 `n=5` | ATE **2.455726**；drops 2 / ids 91；skip 2 |
| MH_05 探针 `n=8/10` | ≈3.052（略优于 ④f） |
| MH_01 `n=5` | ATE **0.0987839** ≤ 0.098784；`reanchors=0`；`drops_skipped=0` |

## 矩阵结果（探针阶段，scratch `/tmp/mh05_zombie_age`，当时 hash `a5e90dc7`）

| n | ATE | RPE | drops / ids | skip | cheir | 备注 |
|---:|---:|---:|---:|---:|---:|---|
| 0 | **3.056878** | 0.772423 | 0 / 0 | 1 | 29 | ≡④f |
| 5 | **2.455726** | 0.711862 | 2 / 91 | 2 | 65 | 选为默认 |
| 8 | 3.051629 | 0.772162 | 1 / 32 | 1 | 29 | |
| 10 | 3.052424 | 0.772334 | 1 / 32 | 1 | 29 | |

产品化后默认路径 `config_hash` = **`773ea011`**（相对 ④f `a5e90dc7` 新增
`session.zombie_drop_age=5`）。全序列未跑。
