# M3.3 Slice ④ / ④b / ④c / ④d / ④e / ④f / ④g 基线（外点剔除 + 二次 LM + cull-track-drop + 多轮 reopt + skip-drop + 延后 zombie-drop）

日期：2026-08-01（④/④b）；2026-08-02（④c / ④d / ④e / ④f / ④g / 回退）  
状态：**默认行为回 ④f** — ④g `pending_drop` 编排已删除（证伪作 ATE 刀；见
§12.6 与 [postmortem](m3.3-slice4g-postmortem.md)）；`deferred_*` 计数合同保留
（恒 0）；hash 仍 `a5e90dc7`。④g 门控史：MH_01 PASS / MH_05 FAIL（ATE
**4.565065**=④e；§12）。④f 为当前 ATE 锚（MH_01+MH_05 软门通过，§11）。  
相关：

- roadmap [M3.3](../roadmap.md)
- 设计 [`m3.3-slice4-outlier-cull-design.md`](m3.3-slice4-outlier-cull-design.md)
- ④b 设计 [`m3.3-slice4b-outlier-reopt-design.md`](m3.3-slice4b-outlier-reopt-design.md)
- ④c 设计 [`m3.3-slice4c-cull-track-drop-design.md`](m3.3-slice4c-cull-track-drop-design.md)
- 开源对照 [`m3.3-slice4-outlier-cull-open-source-refs.md`](m3.3-slice4-outlier-cull-open-source-refs.md)
- 复生开源对照 [`m3.3-cull-id-rebirth-open-source-refs.md`](m3.3-cull-id-rebirth-open-source-refs.md)
- 计划 [`2026-08-01_m3.3_slice4_outlier_cull_840bf39c.plan.md`](../plans/2026-08-01_m3.3_slice4_outlier_cull_840bf39c.plan.md)
- ④b 计划 [`2026-08-01_m3.3_slice4b_outlier_reopt_4d57d1fc.plan.md`](../plans/2026-08-01_m3.3_slice4b_outlier_reopt_4d57d1fc.plan.md)
- ④c 计划 [`2026-08-02_m3.3_slice4c_cull_track_drop_5a3a4e09.plan.md`](../plans/2026-08-02_m3.3_slice4c_cull_track_drop_5a3a4e09.plan.md)
- ④d 设计 [`m3.3-slice4d-cull-threshold-design.md`](m3.3-slice4d-cull-threshold-design.md)
- ④d 计划 [`2026-08-02_m3.3_slice4d_cull_threshold_ecdd49d4.plan.md`](../plans/2026-08-02_m3.3_slice4d_cull_threshold_ecdd49d4.plan.md)
- ④e 设计 [`m3.3-slice4e-multiround-reopt-design.md`](m3.3-slice4e-multiround-reopt-design.md)
- ④e 开源对照 [`m3.3-slice4e-multiround-open-source-refs.md`](m3.3-slice4e-multiround-open-source-refs.md)
- ④e 计划 [`2026-08-02_m3.3_slice4e_multiround_reopt_e0517b9a.plan.md`](../plans/2026-08-02_m3.3_slice4e_multiround_reopt_e0517b9a.plan.md)
- ④f 设计 [`m3.3-slice4f-skip-drop-design.md`](m3.3-slice4f-skip-drop-design.md)
- ④g 设计 [`m3.3-slice4g-zombie-drop-design.md`](m3.3-slice4g-zombie-drop-design.md)
- ④g 事后诊断 [`m3.3-slice4g-postmortem.md`](m3.3-slice4g-postmortem.md)
- 对照 [`m3.3-slice3-baseline.md`](m3.3-slice3-baseline.md)（`b712c91` / `default_8a9236e0`）
- issue [#24](https://github.com/Nothand0212/phad-vio/issues/24)

本文档描述当前约定，不是绝对约束，会随项目开发修订。

## 1. 快照身份（MH_05 诊断 run）

| 项 | 值 |
|---|---|
| git commit | `b6fbcb6`（`fix(apps): keep outlier cull out of session warnings`；含 Tasks 1–3） |
| 产物目录 | `b6fbcb6`（clean 树） |
| config | `default` / hash `bc8b10e2` |
| bench root | `/home/lin/Projects/data/phad-bench` |
| 产物路径 | `MH_05_difficult/b6fbcb6/default_bc8b10e2/` |
| 数据集 | EuRoC ASL native：`…/euroc/native/MH_05_difficult` |
| 工具 | `phad_vo_bench` + `phad::eval`（ATE / RPE Δt=1 s，SE3 `-a` 对齐） |
| 参数权威源 | 产物 `meta.json` 的 `config` / `config_canonical_text`（与 `config_hash` 同源） |

### 1.1 运行时参数快照

来源：`MH_05_difficult/b6fbcb6/default_bc8b10e2/meta.json`。

相对 Slice ③（`8a9236e0`）**新增**两个 estimator 外点剔除键：

| 键 | 值 | 说明 |
|---|---|---|
| `estimator.enable_outlier_cull` | `true` | 优化后平均重投影永久删点 |
| `estimator.outlier_avg_reproj_px` | `3` | 平均重投影阈值（像素） |

`config_canonical_text`（`config_hash = bc8b10e2`）：

```text
estimator.enable_outlier_cull=true
estimator.enable_pnp_init=true
estimator.enable_reanchor=true
estimator.huber_k_px=3
estimator.min_landmark_observations=2
estimator.min_pnp_inliers=10
estimator.min_seed_observations=10
estimator.min_shared_landmarks=10
estimator.outlier_avg_reproj_px=3
estimator.pnp_confidence=0.98999999999999999
estimator.pnp_reproj_px=2
estimator.prior_rotation_sigma_rad=0.0001
estimator.prior_translation_sigma_m=0.0001
estimator.stereo_sigma_px=1
estimator.use_constant_velocity_init=true
estimator.window_size=10
eval.max_dt_ms=2.5
eval.min_match_rate=0.5
eval.rpe_delta_s=1
session.dataset_format=euroc
tracker.forward_backward_px=0.5
tracker.lk_pyramid_levels=3
tracker.lk_window_px=21
tracker.mask_radius_px=20
tracker.max_depth_m=40
tracker.max_epipolar_px=1.5
tracker.max_tracks=200
tracker.min_depth_m=0.29999999999999999
tracker.min_disparity_px=0.5
tracker.min_distance_px=20
tracker.quality_level=0.01
tracker.stereo_bidir_px=0.5
tracker.stereo_check_bidir=true
tracker.stereo_row_tol_px=0
tracker.stereo_sad_half_win_px=7
tracker.stereo_uniq_ratio=0.5
```

复现：checkout `b6fbcb6`（或其后含同默认的 commit），默认 `phad_vo_bench`；
目录段应为 `<commit>/default_bc8b10e2`。

## 2. 一句话结论

VINS 型「优化后平均重投影永久删点、不重优」落地后，MH_05 **离开** Slice ③
的 `~1e4` 发散（ATE 33179→**43.29** m，`cheirality` 2055→**151**），重复删除率
`70/70=1.0`，`reanchors=2`（豁免、非暴增）。但 ATE **未回到 < 1 m**（参照
Slice ② ≈0.40），大剔后本帧 `reproj_rms_after_cull_px` 仍可高达数十～数百 px，
随后出现长 failed 簇——诊断门判定 **不够**，需本帧二次 LM；**不**跑其余序列。

## 3. MH_05 诊断门

证据路径：
`/home/lin/Projects/data/phad-bench/MH_05_difficult/b6fbcb6/default_bc8b10e2/`
（`summary.json` / `diag.csv` / `meta.json`）。

### 3.1 关键数字（相对 Slice ③ `b712c91`）

| 项 | Slice ③ | Slice ④（本 run） |
|---|---:|---:|
| status | completed_with_warnings | completed_with_failures |
| ATE trans RMSE (m) | 33179.50 | **43.2888** |
| RPE trans RMSE (m) | 15591.19 | 0.9291 |
| cheirality | 2055 | **151** |
| outliers_culled | — | 70 |
| outliers_culled_unique | — | 70 |
| culled / unique | — | **1.0** |
| reanchors / segments | 0 / 1 | **2 / 3** |
| completion | 0.998 | 0.884 |
| ok / image_frames | 2268 / 2273 | 2010 / 2273 |
| failed / rejected | 0 / 5 | 257 / 6 |
| pnp_successes / fallbacks | 2229 / 38 | 1979 / 28 |
| wall_s / rtf | ~280 / 0.41 | 251.3 / 0.452 |

### 3.2 预定义判据对照

| 判据 | 够 | 实测 | 结果 |
|---|---|---|---|
| ATE | 离开 ~1e4，回到 **&lt; 1 m**（参照 S② ≈0.40） | 43.29 m（已离开 1e4，**未** &lt;1 m） | **不够** |
| cheirality | 显著下降（远离 2055） | 151 | 改善 |
| 剔点时间线 | 集中前段，后期不再大量剔 | 仅 6 帧剔点；最大一刀在 i=1487（Q3，culled=37），非前段集中 | **不够**（非空转狂剔，但不符合「前段集中」） |
| 重复删除率 | `culled/unique` ≈ 1 | 70/70 = 1.0 | OK |
| reanchors | 可为 &gt;0（豁免）；暴增→不够 | 2 / segments=3 | 豁免 OK（非暴增） |

**总判定：不够** — 「够」要求 ATE &lt; 1 m 且剔点前段集中，二者均未满足；
停止全序列出口，标明需本帧二次 LM（设计讨论 B/C）。

### 3.3 `diag.csv` 时间线备忘

- 全 run 仅 **6** 帧 `outliers_culled > 0`（总剔 70，无重复删）：

| frame i | segment | culled | rms_after | rms_after_cull | cheirality |
|---:|---:|---:|---:|---:|---:|
| 425 | 0 | 24 | 297.1 | **60.7** | 16 |
| 931 | 1 | 1 | 2.35 | 0.43 | 0 |
| 1487 | 1 | 37 | 415.0 | **154.1** | 56 |
| 1489 | 1 | 6 | 468.3 | **337.4** | 15 |
| 1677 | 2 | 1 | 1.78 | 0.56 | 0 |
| 1835 | 2 | 1 | 0.53 | 0.46 | 0 |

- 四分位 culled 合计：Q1=24，Q2=1，Q3=**44**，Q4=1 — 后半仍有大剔。
- 大剔后本帧 cull-RMS 仍极高（60 / 154 / 337 px），说明「只删不重优」未能
  把当前位姿从毒点拉力中拉回。
- 随后 failed 簇：`[429,15]`、`[445,9]`、`[455,101]`、`[1490,132]`（紧接
  i=1487/1489 大剔），completion 降至 0.884。
- re-anchor：`segment_id` 在 i=556、i=1622 切换（`reanchors=2`），属豁免范围。

## 4. 全序列表（Slice ④）

**未跑。** MH_05 诊断门不够 → 按设计停止扩 scope；待二次 LM / 设计 B/C
决议后再开 Task 5。

## 5. 后续（Slice ④ 当时）

- 讨论并落地本帧二次 LM（或重建图）选项；
- 再跑 MH_05 诊断门；够后再全序列 + 硬门控写完整基线。

---

## 6. Slice ④b MH_05 诊断门（二次 LM）

状态：**不够** — 停止扩 scope，**不**跑其余 10 条 / Task 5。

### 6.1 快照身份

| 项 | 值 |
|---|---|
| git commit | `3855395`（`docs(m3.3): add slice-4b outlier-reopt design and plan`；代码含 Tasks 1–3 至 `2ef2995`） |
| 产物目录 | `3855395`（clean 树） |
| config | `default` / hash `65273570` |
| bench root | `/home/lin/Projects/data/phad-bench` |
| 产物路径 | `MH_05_difficult/3855395/default_65273570/` |
| 数据集 | EuRoC ASL native：`…/euroc/native/MH_05_difficult` |
| 工具 | `phad_vo_bench` + `phad::eval`（ATE / RPE Δt=1 s，SE3 `-a` 对齐） |
| 参数权威源 | 产物 `meta.json` 的 `config` / `config_canonical_text`（与 `config_hash` 同源） |
| 对照 | Slice ④ `b6fbcb6` / `default_bc8b10e2`（ATE 43.29 m） |

### 6.2 运行时参数快照

来源：`MH_05_difficult/3855395/default_65273570/meta.json`。

相对 Slice ④（`bc8b10e2`）**新增** estimator 二次 LM 键：

| 键 | 值 | 说明 |
|---|---|---|
| `estimator.enable_outlier_reopt` | `true` | `outliers_culled >= 4` 时 rebuild + LM₂ |

`config_canonical_text`（`config_hash = 65273570`）：

```text
estimator.enable_outlier_cull=true
estimator.enable_outlier_reopt=true
estimator.enable_pnp_init=true
estimator.enable_reanchor=true
estimator.huber_k_px=3
estimator.min_landmark_observations=2
estimator.min_pnp_inliers=10
estimator.min_seed_observations=10
estimator.min_shared_landmarks=10
estimator.outlier_avg_reproj_px=3
estimator.pnp_confidence=0.98999999999999999
estimator.pnp_reproj_px=2
estimator.prior_rotation_sigma_rad=0.0001
estimator.prior_translation_sigma_m=0.0001
estimator.stereo_sigma_px=1
estimator.use_constant_velocity_init=true
estimator.window_size=10
eval.max_dt_ms=2.5
eval.min_match_rate=0.5
eval.rpe_delta_s=1
session.dataset_format=euroc
tracker.forward_backward_px=0.5
tracker.lk_pyramid_levels=3
tracker.lk_window_px=21
tracker.mask_radius_px=20
tracker.max_depth_m=40
tracker.max_epipolar_px=1.5
tracker.max_tracks=200
tracker.min_depth_m=0.29999999999999999
tracker.min_disparity_px=0.5
tracker.min_distance_px=20
tracker.quality_level=0.01
tracker.stereo_bidir_px=0.5
tracker.stereo_check_bidir=true
tracker.stereo_row_tol_px=0
tracker.stereo_sad_half_win_px=7
tracker.stereo_uniq_ratio=0.5
```

复现：checkout `3855395`（或其后含同默认的 commit），默认 `phad_vo_bench`；
目录段应为 `<commit>/default_65273570`。

### 6.3 关键数字（相对 Slice ④ `b6fbcb6`）

证据路径：
`/home/lin/Projects/data/phad-bench/MH_05_difficult/3855395/default_65273570/`
（`summary.json` / `diag.csv` / `meta.json`）。

| 项 | Slice ④ | Slice ④b（本 run） |
|---|---:|---:|
| status | completed_with_failures | completed_with_warnings |
| ATE trans RMSE (m) | 43.2888 | **3.4488** |
| RPE trans RMSE (m) | 0.9291 | 0.9198 |
| cheirality | 151 | **47** |
| outliers_culled | 70 | 72 |
| outliers_culled_unique | 70 | 66 |
| culled / unique | 1.0 | 1.09 |
| outlier_reopts | — | **2** |
| reanchors / segments | 2 / 3 | **0 / 1** |
| completion | 0.884 | **0.998** |
| ok / image_frames | 2010 / 2273 | 2268 / 2273 |
| failed / rejected | 257 / 6 | **0 / 5** |
| pnp_successes / fallbacks | 1979 / 28 | 2239 / 28 |
| wall_s / rtf | 251.3 / 0.452 | 255.5 / 0.445 |

### 6.4 预定义判据对照

| 判据 | 够 | 实测 | 结果 |
|---|---|---|---|
| ATE | **&lt; 1 m** | **3.45 m**（43.3→3.45，仍 ≥1） | **不够**（决定项） |
| cheirality | 不失控回升到发散水平 | 47（151→47） | OK |
| reopt 有效性 | 大剔帧 LM₂ 后 `after_cull` ≤ ~10 px 或个位数 | i=425: **0.18** px；i=436: **0.26** px（对照 ④ 的 60/154/337） | OK |
| reanchors | 可为 &gt;0（豁免）；暴增→不够 | 0 / segments=1 | OK |

**总判定：不够** — ATE 未回到 &lt; 1 m（设计硬门槛）；reopt 本身有效（大剔后
RMS 压到 &lt;1 px、failed 簇清空、completion 恢复、无 re-anchor），但轨迹误差仍
约 3.5 m → **停止**全序列出口；需多轮 cull/reopt 或其它手段，**不**开 Task 5。

### 6.5 `diag.csv` 时间线备忘

- 全 run **6** 帧 `outliers_culled > 0`；其中 **2** 帧 `>= 4` 触发 reopt
  （`outlier_reopts=2`；`lm_iterations` 21 / 16，明显高于仅 LM₁ 的 3–4）：

| frame i | segment | culled | lm_iter | rms_after | rms_after_cull | cheirality |
|---:|---:|---:|---:|---:|---:|---:|
| 425 | 0 | 24 | 21 | 297.1 | **0.18** | 16 |
| 436 | 0 | 44 | 16 | 204.8 | **0.26** | 8 |
| 931 | 0 | 1 | 3 | 2.36 | 0.45 | 0 |
| 1520 | 0 | 1 | 3 | 1.60 | 0.40 | 0 |
| 1677 | 0 | 1 | 4 | 1.78 | 0.58 | 0 |
| 1835 | 0 | 1 | 3 | 0.53 | 0.46 | 0 |

- 对照 Slice ④：大剔后 RMS 60/154/337 → ④b 压到 **0.18 / 0.26**；④ 的长
  failed 簇（257 failed）在 ④b **消失**（failed=0）。
- `segment_id` 全程 0（`reanchors=0`），无掏空窗口。

### 6.6 全序列表（Slice ④b）

**未跑。** MH_05 诊断门不够 → 按设计停止扩 scope；不跑其余 10 条。

### 6.7 后续

- ④b 二次 LM **有效但不够**（ATE 仍 3.45 m）；讨论多轮 cull/reopt、更强
  外点策略或其它手段后再开诊断门；
- **禁止**在未达 ATE &lt; 1 m 前跑全序列硬门控。

---

## 7. Slice ④c MH_01 硬门（拒复生 + dropTracks）

状态：**不够** — ATE 未达 `no_cull` 锚；健康指标 OK；**不**改
`outlier_avg_reproj_px`；本片不蒙混过门。

### 7.1 快照身份

| 项 | 值 |
|---|---|
| git commit | `de32bd7`（`feat(apps): drop frontend tracks after estimator cull`；含 Tasks 1–3） |
| 产物目录 | `de32bd7`（clean 树） |
| config | `default` / hash `9da4ccd2` |
| bench root | `/home/lin/Projects/data/phad-bench` |
| 产物路径 | `MH_01_easy/de32bd7/default_9da4ccd2/` |
| 数据集 | EuRoC ASL native：`…/euroc/native/MH_01_easy` |
| 工具 | `phad_vo_bench` + `phad::eval`（ATE / RPE Δt=1 s，SE3 `-a` 对齐） |
| 参数权威源 | 产物 `meta.json` 的 `config` / `config_canonical_text`（与 `config_hash` 同源） |
| 对照 | tip ⑤前锚 cull crash ~43.48 m；`no_cull_full` 0.099263（`c25d1f99`）；block-only（同 hash、无 dropTracks）0.715866 |

### 7.2 运行时参数快照

来源：`MH_01_easy/de32bd7/default_9da4ccd2/meta.json`。

相对 Slice ④b（`65273570`）**新增** estimator 拒复生键（默认 `true`）：

| 键 | 值 | 说明 |
|---|---|---|
| `estimator.block_culled_rebirth` | `true` | 阻断已 cull id 的 stereo-backproject 复生；配合 session `dropTracks` |

`config_canonical_text`（`config_hash = 9da4ccd2`）：

```text
estimator.block_culled_rebirth=true
estimator.enable_outlier_cull=true
estimator.enable_outlier_reopt=true
estimator.enable_pnp_init=true
estimator.enable_reanchor=true
estimator.huber_k_px=3
estimator.min_landmark_observations=2
estimator.min_pnp_inliers=10
estimator.min_seed_observations=10
estimator.min_shared_landmarks=10
estimator.outlier_avg_reproj_px=3
estimator.pnp_confidence=0.98999999999999999
estimator.pnp_reproj_px=2
estimator.prior_rotation_sigma_rad=0.0001
estimator.prior_translation_sigma_m=0.0001
estimator.stereo_sigma_px=1
estimator.use_constant_velocity_init=true
estimator.window_size=10
eval.max_dt_ms=2.5
eval.min_match_rate=0.5
eval.rpe_delta_s=1
session.dataset_format=euroc
tracker.forward_backward_px=0.5
tracker.lk_pyramid_levels=3
tracker.lk_window_px=21
tracker.mask_radius_px=20
tracker.max_depth_m=40
tracker.max_epipolar_px=1.5
tracker.max_tracks=200
tracker.min_depth_m=0.29999999999999999
tracker.min_disparity_px=0.5
tracker.min_distance_px=20
tracker.quality_level=0.01
tracker.stereo_bidir_px=0.5
tracker.stereo_check_bidir=true
tracker.stereo_row_tol_px=0
tracker.stereo_sad_half_win_px=7
tracker.stereo_uniq_ratio=0.5
```

复现：checkout `de32bd7`（或其后含同默认的 commit），默认 `phad_vo_bench`；
目录段应为 `<commit>/default_9da4ccd2`。

### 7.3 关键数字

证据路径：
`/home/lin/Projects/data/phad-bench/MH_01_easy/de32bd7/default_9da4ccd2/`
（`summary.json` / `diag.csv` / `meta.json`）。

| 项 | tip ⑤前锚 | block_rebirth_full | no_cull_full | Slice ④c（本 run） |
|---|---:|---:|---:|---:|
| ATE trans RMSE (m) | ~43.48 | 0.715866 | **0.099263** | **0.120247** |
| RPE trans RMSE (m) | — | — | — | 0.019493 |
| reanchors / segments | — | — | — | **0 / 1** |
| failed | — | — | — | **0** |
| ok / image_frames | — | — | — | 3682 / 3682 |
| completion_rate | — | — | — | 1 |
| cheirality | — | — | — | 36 |
| outliers_culled / unique | — | — | — | 57 / 57 |
| outlier_reopts | — | — | — | 1 |
| wall_s / rtf | — | — | — | 458.2 / 0.402 |
| `block_culled_rebirth` | — | true（无 dropTracks） | — | **true**（+ dropTracks） |

`both_on`（④b 式 cull+reopt、无拒复生）对照 ATE ≈6.40、failed=183（诊断用，非本门）。

### 7.4 预定义判据对照

| 判据 | 够 | 实测 | 结果 |
|---|---|---|---|
| ATE | ≤ **0.099263** m（`no_cull_full` 锚） | **0.120247** m | **不够**（决定项） |
| segments / reanchors | 1 / 0 | 1 / 0 | OK |
| failed / 长 failed 簇 | 0（或不构成吸收态） | 0 | OK |
| `block_culled_rebirth` | `true` 在 `config_canonical_text` | true（hash `9da4ccd2`） | OK |

**总判定：不够** — 健康恢复（相对 tip ~43.48 与 block-only 0.72，ATE 落到
0.12；seg/re/failed 全绿），但未回到 `no_cull` 锚 0.099263 → 硬门 **不够**。
`dropTracks` 相对 block-only 有效（0.72→0.12）。**禁止**放宽
`outlier_avg_reproj_px` 蒙混过门；后续另开阈值片或其它手段。

### 7.5 后续

- MH_05 诊断见 §8；软判定 **停** Task 6 全序列；
- 本片 **不**改 cull 阈值。

---

## 8. Slice ④c MH_05 诊断（拒复生 + dropTracks）

状态：**软判定停全序列** — ATE 相对 ④b 变差（4.56 vs 3.45 m）；非 ~1e4 /
completion 崩坏；**不**进 Task 6。

### 8.1 快照身份

| 项 | 值 |
|---|---|
| git commit | `d5118e0`（`docs(m3.3): record slice-4c MH_01 cull-track-drop hard gate`；代码含 Tasks 1–3 至 `de32bd7`） |
| 产物目录 | `d5118e0`（clean 树） |
| config | `default` / hash `9da4ccd2`（与 MH_01 ④c 同） |
| bench root | `/home/lin/Projects/data/phad-bench` |
| 产物路径 | `MH_05_difficult/d5118e0/default_9da4ccd2/` |
| 数据集 | EuRoC ASL native：`…/euroc/native/MH_05_difficult` |
| 工具 | `phad_vo_bench` + `phad::eval`（ATE / RPE Δt=1 s，SE3 `-a` 对齐） |
| 参数权威源 | 产物 `meta.json` 的 `config` / `config_canonical_text`（与 `config_hash` 同源） |
| 对照 | Slice ④b `3855395` / `default_65273570`（ATE 3.448773 m） |

### 8.2 运行时参数快照

来源：`MH_05_difficult/d5118e0/default_9da4ccd2/meta.json`。

相对 Slice ④b（`65273570`）**新增** estimator 拒复生键（与 MH_01 ④c 同 hash）：

| 键 | 值 | 说明 |
|---|---|---|
| `estimator.block_culled_rebirth` | `true` | 阻断已 cull id 的 stereo-backproject 复生；配合 session `dropTracks` |

`config_canonical_text`（`config_hash = 9da4ccd2`）：

```text
estimator.block_culled_rebirth=true
estimator.enable_outlier_cull=true
estimator.enable_outlier_reopt=true
estimator.enable_pnp_init=true
estimator.enable_reanchor=true
estimator.huber_k_px=3
estimator.min_landmark_observations=2
estimator.min_pnp_inliers=10
estimator.min_seed_observations=10
estimator.min_shared_landmarks=10
estimator.outlier_avg_reproj_px=3
estimator.pnp_confidence=0.98999999999999999
estimator.pnp_reproj_px=2
estimator.prior_rotation_sigma_rad=0.0001
estimator.prior_translation_sigma_m=0.0001
estimator.stereo_sigma_px=1
estimator.use_constant_velocity_init=true
estimator.window_size=10
eval.max_dt_ms=2.5
eval.min_match_rate=0.5
eval.rpe_delta_s=1
session.dataset_format=euroc
tracker.forward_backward_px=0.5
tracker.lk_pyramid_levels=3
tracker.lk_window_px=21
tracker.mask_radius_px=20
tracker.max_depth_m=40
tracker.max_epipolar_px=1.5
tracker.max_tracks=200
tracker.min_depth_m=0.29999999999999999
tracker.min_disparity_px=0.5
tracker.min_distance_px=20
tracker.quality_level=0.01
tracker.stereo_bidir_px=0.5
tracker.stereo_check_bidir=true
tracker.stereo_row_tol_px=0
tracker.stereo_sad_half_win_px=7
tracker.stereo_uniq_ratio=0.5
```

复现：checkout `d5118e0`（或其后含同默认的 commit），默认 `phad_vo_bench`；
目录段应为 `<commit>/default_9da4ccd2`。

### 8.3 关键数字（相对 Slice ④b `3855395`）

证据路径：
`/home/lin/Projects/data/phad-bench/MH_05_difficult/d5118e0/default_9da4ccd2/`
（`summary.json` / `diag.csv` / `meta.json`）。

| 项 | Slice ④b | Slice ④c（本 run） |
|---|---:|---:|
| status | completed_with_warnings | completed_with_warnings |
| ATE trans RMSE (m) | 3.448773 | **4.564954** |
| RPE trans RMSE (m) | 0.919759 | 1.250271 |
| cheirality | 47 | **25** |
| outliers_culled / unique | 72 / 66 | 28 / 28 |
| outlier_reopts | 2 | 1 |
| reanchors / segments | 0 / 1 | **0 / 1** |
| completion | 0.998 | **0.998**（2268/2273） |
| failed / rejected | 0 / 5 | **0** / — |
| pnp_successes / fallbacks | 2239 / 28 | 2238 / 29 |
| wall_s / rtf | 255.5 / 0.445 | 255.3 / 0.445 |

warnings：`vo segments summary: segments=1 reanchors=0 seed_rejected=1`；
`vo pnp summary: pnp_successes=2238 pnp_fallbacks=29`。

### 8.4 软判定

| 观察 | 倾向 | 实测 |
|---|---|---|
| 相对 3.45 m 持平或更好 | 可进 Task 6 | ATE **4.565**（变差） |
| 回到 ~1e4 或 completion 崩 | **停全序列** | **未**回 ~1e4；completion ≈0.998 未崩 |

**总判定：停 Task 6 全序列** — ATE 相对 ④b 变差（4.56 vs 3.45 m），不满足
「持平或更好」；同时非发散崩坏（非 ~1e4、failed=0、seg/re=1/0、completion
健康）。④c 可标 MH_01 健康恢复 + MH_05 诊断完成；**禁止**在未决前跑全序列。

### 8.5 全序列表（Slice ④c）

**未跑。** MH_05 软判定停全序列 → 不进 Task 6。

### 8.6 后续

- ④c track-drop 在 MH_05 上 ATE 变差但未崩；后续另议阈值片 / 其它手段；
- 本片 **不**改 cull 阈值；**不**跑其余 10 条。
- ④d 阈值片见 §9。

---

## 9. Slice ④d Mean-cull 阈值（`outlier_avg_reproj_px`）

状态：**MH_01 硬门通过** — 扫参网格选定 `T*=4.0`（过门中最紧）；默认
3.0→**4.0**；MH_05 与 ④c 持平（ATE **4.565**）→ 软门未停 → 全序列只记录。

### 9.1 MH_01 扫参表（commit `9feb6d4`，CLI 覆盖）

硬门：ATE ≤ **0.099263**；`seg/re=1/0`；`failed=0`。

| T | config_hash | ATE | seg/re | failed | culled/unique | 硬门 |
|---|---|---:|---|---:|---:|---|
| 3.0 | `9da4ccd2` | 0.120247 | 1/0 | 0 | 57/57 | FAIL |
| 3.5 | `b678031f` | 24.372764 | 2/1 | 106 | 73/73 | FAIL |
| **4.0** | `85c97158` | **0.098784** | 1/0 | 0 | 3/3 | **PASS** |
| 5.0 | `1d81fd21` | 0.096890 | 1/0 | 0 | 3/3 | PASS |
| 6.0 | `ccc79d34` | 3.700699 | 1/0 | 0 | 151/151 | FAIL |

选默认规则：过门中取最小 T → **`T*=4.0`**。  
备注：阈值响应非单调（3.5 / 6.0 发散或大恶化）；4.0/5.0 几乎不剔点
（culled=3）。

### 9.2 选定默认身份（commit `79505f8` / `default_85c97158`）

| 项 | 值 |
|---|---|
| git commit | `79505f8`（`fix(estimator): raise outlier_avg_reproj_px default to 4.0`） |
| config | `default` / hash `85c97158` |
| 相对 ④c（`9da4ccd2`）增量键 | `estimator.outlier_avg_reproj_px`：`3` → **`4`** |
| bench root | `/home/lin/Projects/data/phad-bench` |
| 产物路径模板 | `<bench_root>/<sequence>/79505f8/default_85c97158/` |

`config_canonical_text`（`config_hash = 85c97158`）：

```text
estimator.block_culled_rebirth=true
estimator.enable_outlier_cull=true
estimator.enable_outlier_reopt=true
estimator.enable_pnp_init=true
estimator.enable_reanchor=true
estimator.huber_k_px=3
estimator.min_landmark_observations=2
estimator.min_pnp_inliers=10
estimator.min_seed_observations=10
estimator.min_shared_landmarks=10
estimator.outlier_avg_reproj_px=4
estimator.pnp_confidence=0.98999999999999999
estimator.pnp_reproj_px=2
estimator.prior_rotation_sigma_rad=0.0001
estimator.prior_translation_sigma_m=0.0001
estimator.stereo_sigma_px=1
estimator.use_constant_velocity_init=true
estimator.window_size=10
eval.max_dt_ms=2.5
eval.min_match_rate=0.5
eval.rpe_delta_s=1
session.dataset_format=euroc
tracker.forward_backward_px=0.5
tracker.lk_pyramid_levels=3
tracker.lk_window_px=21
tracker.mask_radius_px=20
tracker.max_depth_m=40
tracker.max_epipolar_px=1.5
tracker.max_tracks=200
tracker.min_depth_m=0.29999999999999999
tracker.min_disparity_px=0.5
tracker.min_distance_px=20
tracker.quality_level=0.01
tracker.stereo_bidir_px=0.5
tracker.stereo_check_bidir=true
tracker.stereo_row_tol_px=0
tracker.stereo_sad_half_win_px=7
tracker.stereo_uniq_ratio=0.5
```

### 9.3 MH_01 / MH_05 门控（默认 4.0）

| 门 | 要求 | 实测 | 结果 |
|---|---|---|---|
| MH_01 ATE | ≤ 0.099263 | **0.098784** | **PASS** |
| MH_01 seg/re/failed | 1/0/0 | 1/0/0 | **PASS** |
| MH_05 vs ④c | 不劣于 4.565 | **4.565065** | 持平 → 进全序列 |
| MH_05 vs ④b | 观察（3.449） | 4.565 | 仍差；不硬门 &lt;1 m |

### 9.4 全序列表（`79505f8` / `default_85c97158`）

| sequence | status | ate_trans_rmse | rpe_trans_rmse | completion | segments | reanchors | ok / image_frames | outliers_culled / unique | outlier_reopts |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| MH_01_easy | completed | 0.098784 | 0.018529 | 1.000 | 1 | 0 | 3682 / 3682 | 3 / 3 | 0 |
| MH_02_easy | completed_with_failures | 276879.639366 | 84589.762155 | 0.770 | 5 | 4 | 2341 / 3040 | 489 / 489 | 44 |
| MH_03_medium | completed_with_warnings | 0.712381 | 0.155634 | 1.000 | 1 | 0 | 2700 / 2700 | 23 / 23 | 1 |
| MH_04_difficult | completed_with_warnings | 0.260463 | 0.055328 | 1.000 | 1 | 0 | 2032 / 2032 | 3 / 3 | 0 |
| MH_05_difficult | completed_with_warnings | 4.565065 | 1.250271 | 0.998 | 1 | 0 | 2268 / 2273 | 28 / 28 | 1 |
| V1_01_easy | completed_with_warnings | 0.172690 | 0.075828 | 0.999 | 1 | 0 | 2908 / 2912 | 18 / 18 | 0 |
| V1_02_medium | completed_with_failures | 25462.216178 | 5059.431125 | 0.791 | 3 | 2 | 1352 / 1710 | 445 / 445 | 14 |
| V1_03_difficult | completed_with_failures | 16659720.362561 | 1351820.141996 | 0.654 | 43 | 42 | 1405 / 2149 | 148 / 148 | 6 |
| V2_01_easy | completed_with_failures | 6.019109 | 0.998048 | 0.842 | 3 | 2 | 1919 / 2280 | 48 / 48 | 2 |
| V2_02_medium | completed_with_failures | 6.550058 | 1.067783 | 0.923 | 5 | 4 | 2167 / 2348 | 37 / 37 | 2 |
| V2_03_difficult | completed_with_failures | 9.407120 | 2.094256 | 0.453 | 13 | 12 | 870 / 1921 | 19 / 19 | 0 |

### 9.5 判定与后续

- **④d 够**（就 MH_01 硬门与默认选定而言）；MH_05 未回到 &lt;1 m / ④b 水平。
- 已知债（只记录）：MH_02 / V1_02 / V1_03 发散；多序列 `reanchors` 上升。
- 下一刀建议 **④e**（多轮 cull↔LM / 观测级外点），而非直接 ⑤；⑤ 的
  MH_01 门控已满足，但仍与 M4 边界耦合，需单独对齐。
- ④e 多轮 reopt 结果见 §10。

---

## 10. Slice ④e 多轮 cull↔LM（`max_outlier_reopts`）

状态：**部分完成** — 编码完成（默认 `max_outlier_reopts=3`）；MH_01 不回归
（ATE **0.0987839** ≤ 0.098784；seg/re/failed=1/0/0）；MH_05 与 ④d **持平**
（ATE **4.565065**，相对 `79505f8`/`85c97158` bit-identical metrics）→ 无软改善
→ **停全序列**。多轮预算在 MH_05 上未被动用（仅 1 次成功 reopt）。

### 10.1 快照身份

| 项 | 值 |
|---|---|
| git commit | `0ced28b`（`feat(apps): wire max_outlier_reopts and count reopt rounds`） |
| 产物目录 | `0ced28b`（clean 树） |
| config | `default` / hash `3a21162e` |
| 相对 ④d（`85c97158`）增量键 | `estimator.max_outlier_reopts`：**`3`**（新键） |
| bench root | `/home/lin/Projects/data/phad-bench` |
| 产物路径模板 | `<bench_root>/<sequence>/0ced28b/default_3a21162e/` |
| 数据集 | EuRoC ASL native：`…/euroc/native/<sequence>` |
| 工具 | `phad_vo_bench` + `phad::eval`（ATE / RPE Δt=1 s，SE3 `-a` 对齐） |
| 参数权威源 | 产物 `meta.json` 的 `config` / `config_canonical_text`（与 `config_hash` 同源） |
| 对照 | Slice ④d `79505f8` / `default_85c97158`（MH_01 ATE 0.098784；MH_05 4.565） |

### 10.2 运行时参数快照

来源：`MH_01_easy/0ced28b/default_3a21162e/meta.json`（与 MH_05 同 hash）。

相对 Slice ④d（`85c97158`）**新增** estimator 多轮 reopt 键：

| 键 | 值 | 说明 |
|---|---|---|
| `estimator.max_outlier_reopts` | `3` | 本帧成功 rebuild+LM 的最大轮数；达顶后仍可再 cull |

`config_canonical_text`（`config_hash = 3a21162e`）：

```text
estimator.block_culled_rebirth=true
estimator.enable_outlier_cull=true
estimator.enable_outlier_reopt=true
estimator.enable_pnp_init=true
estimator.enable_reanchor=true
estimator.huber_k_px=3
estimator.max_outlier_reopts=3
estimator.min_landmark_observations=2
estimator.min_pnp_inliers=10
estimator.min_seed_observations=10
estimator.min_shared_landmarks=10
estimator.outlier_avg_reproj_px=4
estimator.pnp_confidence=0.98999999999999999
estimator.pnp_reproj_px=2
estimator.prior_rotation_sigma_rad=0.0001
estimator.prior_translation_sigma_m=0.0001
estimator.stereo_sigma_px=1
estimator.use_constant_velocity_init=true
estimator.window_size=10
eval.max_dt_ms=2.5
eval.min_match_rate=0.5
eval.rpe_delta_s=1
session.dataset_format=euroc
tracker.forward_backward_px=0.5
tracker.lk_pyramid_levels=3
tracker.lk_window_px=21
tracker.mask_radius_px=20
tracker.max_depth_m=40
tracker.max_epipolar_px=1.5
tracker.max_tracks=200
tracker.min_depth_m=0.29999999999999999
tracker.min_disparity_px=0.5
tracker.min_distance_px=20
tracker.quality_level=0.01
tracker.stereo_bidir_px=0.5
tracker.stereo_check_bidir=true
tracker.stereo_row_tol_px=0
tracker.stereo_sad_half_win_px=7
tracker.stereo_uniq_ratio=0.5
```

复现：checkout `0ced28b`（或其后含同默认的 commit），默认 `phad_vo_bench`；
目录段应为 `<commit>/default_3a21162e`。

### 10.3 MH_01 / MH_05 门控

证据路径：

- MH_01：`/home/lin/Projects/data/phad-bench/MH_01_easy/0ced28b/default_3a21162e/`
- MH_05：`/home/lin/Projects/data/phad-bench/MH_05_difficult/0ced28b/default_3a21162e/`

| 门 | 要求 | 实测 | 结果 |
|---|---|---|---|
| MH_01 ATE | ≤ 0.098784 | **0.0987839** | **PASS** |
| MH_01 seg/re/failed | 1/0/0 | 1/0/0 | **PASS** |
| MH_01 culled / unique | — | 3 / 3 | OK |
| MH_01 `outlier_reopts` | — | **0**（无帧触发 `culled_round>=4`） | OK |
| MH_05 软改善 | 相对 ④d 4.565 明显下降 | **4.565065**（持平 / bit-identical） | **否** |
| MH_05 硬够 | ATE &lt; 1 m | 4.565065 | **否** |
| MH_05 `outlier_reopts` | — | **1**（全序列仅 1 次成功重优；无 `rounds>1` 帧） | 多轮未动用 |

| 项 | Slice ④d | Slice ④e（本 run） |
|---|---:|---:|
| MH_01 ATE (m) | 0.098784 | **0.0987839** |
| MH_01 RPE (m) | 0.018529 | 0.0185291 |
| MH_01 culled / unique | 3 / 3 | 3 / 3 |
| MH_01 outlier_reopts | 0 | 0 |
| MH_05 ATE (m) | 4.565065 | **4.565065** |
| MH_05 RPE (m) | 1.250271 | 1.250271 |
| MH_05 culled / unique | 28 / 28 | 28 / 28 |
| MH_05 outlier_reopts | 1 | **1** |
| MH_05 cheirality | 25 | 25 |
| MH_05 pnp_successes / fallbacks | 2238 / 29 | 2238 / 29 |
| MH_05 segments / reanchors / failed | 1 / 0 / 0 | 1 / 0 / 0 |

MH_05 解读：`max_outlier_reopts=3` 在本序列上与 ④d/`max=1` 行为重合——首轮
LM₂ 后再 cull 未再触发 `culled_round>=4`，多轮预算未动用。

### 10.4 全序列表（Slice ④e）

**未跑。** MH_05 无软改善（ATE 与 ④d 持平，非 &lt;1 m）→ 按设计停止扩 scope；
不跑其余 10 条。

### 10.5 `outlier_reopts` 次数语义

`summary.json` 的 `outlier_reopts` = Σ 各帧 `outlier_reopt_rounds`（**成功
rebuild+LM 的次数**，不是「发生过 reopt 的帧数」）。例：某帧 `rounds=2` 计入
2；全序列仅一帧 `rounds=1` 则 `outlier_reopts=1`。

### 10.6 判定与后续

- **④e 部分完成**：编码与契约落地；MH_01 硬门不回归；MH_05 **不够**（持平）。
- **禁止**在未达软改善 / 硬够前跑全序列。
- 下一刀建议：去 Huber 末轮 / 观测级外点续片（单独设计 + 开源对照）；**不开** ⑤
  （④e 不够消化 MH_05；⑤ 仍与 M4 预积分 / `est.tum` 语义耦合，须单独对齐后再开）。
- ④f skip-drop 结果见 §11。

---

## 11. Slice ④f 大剔跳过 dropTracks（`skip_drop_min_culled`）

状态：**已完成** — 编码完成（默认 `skip_drop_min_culled=4`）；MH_01 硬门 **PASS**；
MH_05 软门 **PASS**（ATE 相对 ④e 明显下降，朝 no_drop A/B 方向）；**全序列未跑**。

### 11.1 快照身份

| 项 | 值 |
|---|---|
| git commit（feat） | `5342113`（`feat(apps): skip dropTracks when mean-cull count >= N`） |
| git commit（门控/docs tip） | `c446ac5`（`docs(apps): document Slice ④f skip-drop`；含同默认） |
| 产物目录 | `c446ac5`（clean 树；bench 目录段跟 tip） |
| config | `default` / hash `a5e90dc7` |
| 相对 ④e（`3a21162e`）增量键 | `session.drop_culled_tracks=true`；`session.skip_drop_min_culled=4` |
| bench root | `/home/lin/Projects/data/phad-bench` |
| 产物路径模板 | `<bench_root>/<sequence>/c446ac5/default_a5e90dc7/` |
| 数据集 | EuRoC ASL native：`…/euroc/native/<sequence>` |
| 工具 | `phad_vo_bench` + `phad::eval`（ATE / RPE Δt=1 s，SE3 `-a` 对齐） |
| 参数权威源 | 产物 `meta.json` 的 `config` / `config_canonical_text`（与 `config_hash` 同源） |
| 对照 | Slice ④e `0ced28b` / `default_3a21162e`（MH_01 ATE 0.0987839；MH_05 4.565）；no_drop A/B ATE **3.972** |

### 11.2 运行时参数快照

来源：`MH_01_easy/c446ac5/default_a5e90dc7/meta.json`（与 MH_05 同 hash）。

相对 Slice ④e（`3a21162e`）**新增** session skip-drop 键：

| 键 | 值 | 说明 |
|---|---|---|
| `session.drop_culled_tracks` | `true` | 小剔仍 drop；生产默认 |
| `session.skip_drop_min_culled` | `4` | `outliers_culled ≥ 4` 时跳过本帧 `dropTracks`；`0` = 不跳过 |

`config_canonical_text`（`config_hash = a5e90dc7`）：

```text
estimator.block_culled_rebirth=true
estimator.enable_outlier_cull=true
estimator.enable_outlier_reopt=true
estimator.enable_pnp_init=true
estimator.enable_reanchor=true
estimator.huber_k_px=3
estimator.max_outlier_reopts=3
estimator.min_landmark_observations=2
estimator.min_pnp_inliers=10
estimator.min_seed_observations=10
estimator.min_shared_landmarks=10
estimator.outlier_avg_reproj_px=4
estimator.pnp_confidence=0.98999999999999999
estimator.pnp_reproj_px=2
estimator.prior_rotation_sigma_rad=0.0001
estimator.prior_translation_sigma_m=0.0001
estimator.stereo_sigma_px=1
estimator.use_constant_velocity_init=true
estimator.window_size=10
eval.max_dt_ms=2.5
eval.min_match_rate=0.5
eval.rpe_delta_s=1
session.dataset_format=euroc
session.drop_culled_tracks=true
session.skip_drop_min_culled=4
tracker.forward_backward_px=0.5
tracker.lk_pyramid_levels=3
tracker.lk_window_px=21
tracker.mask_radius_px=20
tracker.max_depth_m=40
tracker.max_epipolar_px=1.5
tracker.max_tracks=200
tracker.min_depth_m=0.29999999999999999
tracker.min_disparity_px=0.5
tracker.min_distance_px=20
tracker.quality_level=0.01
tracker.stereo_bidir_px=0.5
tracker.stereo_check_bidir=true
tracker.stereo_row_tol_px=0
tracker.stereo_sad_half_win_px=7
tracker.stereo_uniq_ratio=0.5
```

复现：checkout `c446ac5`（或其后含同默认的 commit），默认 `phad_vo_bench`；
目录段应为 `<commit>/default_a5e90dc7`。

### 11.3 MH_01 / MH_05 门控

证据路径：

- MH_01：`/home/lin/Projects/data/phad-bench/MH_01_easy/c446ac5/default_a5e90dc7/`
- MH_05：`/home/lin/Projects/data/phad-bench/MH_05_difficult/c446ac5/default_a5e90dc7/`

| 门 | 要求 | 实测 | 结果 |
|---|---|---|---|
| MH_01 ATE | ≤ 0.098784 | **0.0987839** | **PASS** |
| MH_01 seg/re/failed | 1/0/0 | 1/0/0 | **PASS** |
| MH_01 `drops_skipped` | 0（期望） | **0** | **PASS** |
| MH_01 culled / unique | — | 3 / 3 | OK |
| MH_01 `outlier_reopts` | — | 0 | OK |
| MH_05 软改善 | 相对 ④e 4.565 明显下降 | **3.056878**（Δ≈−1.51） | **PASS** |
| MH_05 vs no_drop A/B | 朝 ~3.97 方向 | **3.057** &lt; **3.972** | **PASS** |
| MH_05 `drops_skipped` | ≥ 1 | **1** | **PASS** |
| MH_05 硬够 | ATE &lt; 1 m | 3.057 | **否** |

| 项 | Slice ④e | Slice ④f（本 run） |
|---|---:|---:|
| MH_01 ATE (m) | 0.0987839 | **0.0987839** |
| MH_01 RPE (m) | 0.018529 | 0.018529 |
| MH_01 culled / unique | 3 / 3 | 3 / 3 |
| MH_01 `drops_skipped` | — | **0** |
| MH_01 outlier_reopts | 0 | 0 |
| MH_05 ATE (m) | 4.565065 | **3.056878** |
| MH_05 RPE (m) | 1.250271 | **0.772423** |
| MH_05 culled / unique | 28 / 28 | 28 / 28 |
| MH_05 outlier_reopts | 1 | 1 |
| MH_05 `drops_skipped` | — | **1** |
| MH_05 cheirality | 25 | 29 |
| MH_05 pnp_successes / fallbacks | 2238 / 29 | 2238 / 29 |
| MH_05 segments / reanchors / failed | 1 / 0 / 0 | 1 / 0 / 0 |
| MH_05 completion | 0.998 | **0.998**（2268/2273） |
| MH_05 status | completed_with_warnings | completed_with_warnings |
| MH_05 wall_s / rtf | — | 535.7 / 0.212 |

MH_05 解读：`outliers_culled=24` 大剔帧（i≈425）触发 skip-drop（`drops_skipped=1`），
保留前端 track 连通性；ATE 从 ④e 的 4.565 降至 **3.057**，优于 no_drop A/B 的 3.972，
但仍未达 &lt;1 m 硬门槛。

### 11.4 全序列表（Slice ④f）

**未跑。** 计划 record-only；MH_01 硬门 + MH_05 软门已通过，全序列另议。

### 11.5 判定与后续

- **④f 够**（就 MH_01 硬门与 MH_05 软门而言）：skip-drop 消化 ④c「凡 cull 必 drop」
  导致的 MH_05 回归；MH_01 行为不变（`drops_skipped=0`）。
- MH_05 仍 **3.06 m**（非 &lt;1 m；已优于 ④b ≈3.45）；已知债更新为「软改善后仍 ~3 m，
  形态见 [post-4f 诊断](m3.3-mh05-post4f-diagnosis.md)」。
- **未跑**其余 EuRoC；**不开** ⑤（仍与 M4 预积分 / `est.tum` 语义耦合，须单独对齐）。
- 下一刀曾为 ④g（skip-drop 后延后精确 drop；见
  [Probe B 诊断](m3.3-mh05-probe-b-diagnosis.md) §6）；结果见 §12。

---

## 12. Slice ④g skip-drop 后延后 drop zombie（精确 `dropTracks`）

状态：**门控不够 / 部分完成** — 机制已编码（pending_drop → 下帧 `process` 前
精确 `dropTracks`）；MH_01 硬门 **PASS**；MH_05 软门 **FAIL**（ATE 回到 ④e
平台 **4.565065**；zombie 归零但 shared 未收窄）→ **停全序列**；**不开** ⑤。

### 12.1 快照身份

| 项 | 值 |
|---|---|
| git commit（feat pending） | `806d05b`（`feat(apps): defer dropTracks after skip-drop via pending_drop`） |
| git commit（counters） | `9d6cf2b`（`feat(bench): add deferred_drops counters to summary contract`） |
| git commit（门控树 tip） | `3ee5dea`（`feat(apps): wire deferred_drops into bench summary and probe`） |
| 产物目录 | `3ee5dea`（clean 树；bench 目录段跟 tip） |
| config | `default` / hash `a5e90dc7`（与 ④f **同 hash**） |
| 相对 ④f（`a5e90dc7`）增量 | **行为变、无新 config 键**：skip 当帧仍不 drop；完整 `culled_landmark_ids` 入 `pending_drop`，下帧 `tracker.process` 前冲刷；`summary.robustness.deferred_drops` / `deferred_drop_ids` |
| bench root（scratch） | `/tmp/mh01_4g`、`/tmp/mh05_4g` |
| 产物路径 | `MH_01_easy/3ee5dea/default_a5e90dc7/`；`MH_05_difficult/3ee5dea/default_a5e90dc7/` |
| probe-b | `/tmp/mh05_4g/probe_b.jsonl`（旁路；默认关；不进 `config_hash`） |
| 数据集 | EuRoC ASL native：`…/euroc/native/<sequence>` |
| 工具 | `phad_vo_bench` + `phad::eval`（ATE / RPE Δt=1 s，SE3 `-a` 对齐） |
| 参数权威源 | 产物 `meta.json` 的 `config` / `config_canonical_text`（与 `config_hash` 同源） |
| 对照 | Slice ④f `c446ac5` / `default_a5e90dc7`（MH_01 ATE 0.0987839；MH_05 3.056878）；Slice ④e `0ced28b` / `default_3a21162e`（MH_05 4.565065） |

### 12.2 运行时参数快照

来源：`MH_05_difficult/3ee5dea/default_a5e90dc7/meta.json`（与 MH_01 同 hash）。

相对 Slice ④f：**无新键**；`config_canonical_text` 与 ④f 位相同（hash
`a5e90dc7`）。行为增量见 §12.1（延后 drop）。

| 键 | 值 | 说明 |
|---|---|---|
| `session.drop_culled_tracks` | `true` | 同 ④f |
| `session.skip_drop_min_culled` | `4` | 同 ④f；skip 当帧不立即 drop |
| （无新键） | — | pending_drop 为 apps 默认编排，不进 flatten / hash |

`config_canonical_text`（`config_hash = a5e90dc7`）：

```text
estimator.block_culled_rebirth=true
estimator.enable_outlier_cull=true
estimator.enable_outlier_reopt=true
estimator.enable_pnp_init=true
estimator.enable_reanchor=true
estimator.huber_k_px=3
estimator.max_outlier_reopts=3
estimator.min_landmark_observations=2
estimator.min_pnp_inliers=10
estimator.min_seed_observations=10
estimator.min_shared_landmarks=10
estimator.outlier_avg_reproj_px=4
estimator.pnp_confidence=0.98999999999999999
estimator.pnp_reproj_px=2
estimator.prior_rotation_sigma_rad=0.0001
estimator.prior_translation_sigma_m=0.0001
estimator.stereo_sigma_px=1
estimator.use_constant_velocity_init=true
estimator.window_size=10
eval.max_dt_ms=2.5
eval.min_match_rate=0.5
eval.rpe_delta_s=1
session.dataset_format=euroc
session.drop_culled_tracks=true
session.skip_drop_min_culled=4
tracker.forward_backward_px=0.5
tracker.lk_pyramid_levels=3
tracker.lk_window_px=21
tracker.mask_radius_px=20
tracker.max_depth_m=40
tracker.max_epipolar_px=1.5
tracker.max_tracks=200
tracker.min_depth_m=0.29999999999999999
tracker.min_disparity_px=0.5
tracker.min_distance_px=20
tracker.quality_level=0.01
tracker.stereo_bidir_px=0.5
tracker.stereo_check_bidir=true
tracker.stereo_row_tol_px=0
tracker.stereo_sad_half_win_px=7
tracker.stereo_uniq_ratio=0.5
```

复现：checkout `3ee5dea`（或其后含同默认的 commit），默认 `phad_vo_bench`；
目录段应为 `<commit>/default_a5e90dc7`。可选 `--probe-b <path.jsonl>` 验证 zombie。

### 12.3 MH_01 / MH_05 门控

证据路径（scratch，未入库）：

- MH_01：`/tmp/mh01_4g/MH_01_easy/3ee5dea/default_a5e90dc7/`
- MH_05：`/tmp/mh05_4g/MH_05_difficult/3ee5dea/default_a5e90dc7/`
- probe-b：`/tmp/mh05_4g/probe_b.jsonl`

| 门 | 要求 | 实测 | 结果 |
|---|---|---|---|
| MH_01 ATE | ≤ 0.098784 | **0.0987839** | **PASS** |
| MH_01 seg/re/failed | 1/0/0 | 1/0/0（`reanchors=0`） | **PASS** |
| MH_01 `drops_skipped` | 0（期望） | **0** | **PASS** |
| MH_01 `deferred_drops` / `deferred_drop_ids` | 0 / 0（期望） | **0 / 0** | **PASS** |
| MH_05 软改善 | 相对 ④f 3.056878 改善，或至少不回 ≈4.565 | **4.565065**（= ④e 平台） | **FAIL** |
| MH_05 `drops_skipped` | ≥ 1 | **1** | OK（机制） |
| MH_05 `deferred_drops` / `deferred_drop_ids` | ≥ 1 / ≥ 1 | **1 / 32** | OK（机制；完整 cull 集） |
| MH_05 probe-b zombie | i=425 skip 后 `zombie_track_n` 迅速归零 | i=426+ **zombie=0** | **PASS**（机制） |
| MH_05 probe-b shared | 427–429 相对 ④f（16/29/34）收窄 | **29/43/52**（更宽） | **FAIL** |
| MH_05 硬够 | ATE &lt; 1 m | 4.565065 | **否** |

| 项 | Slice ④f | Slice ④g（本 run） |
|---|---:|---:|
| MH_01 ATE (m) | 0.0987839 | **0.0987839** |
| MH_01 `drops_skipped` | 0 | **0** |
| MH_01 `deferred_drops` / ids | — | **0 / 0** |
| MH_05 ATE (m) | **3.056878** | **4.565065** |
| MH_05 RPE (m) | 0.772423 | **1.250271** |
| MH_05 `drops_skipped` | 1 | **1** |
| MH_05 `deferred_drops` / ids | — | **1 / 32** |
| MH_05 culled / unique | 28 / 28 | 28 / 28 |
| MH_05 outlier_reopts | 1 | 1 |
| MH_05 cheirality | 29 | **25**（= ④e） |
| MH_05 pnp_successes / fallbacks | 2238 / 29 | 2238 / 29 |
| MH_05 segments / reanchors / failed | 1 / 0 / 0 | 1 / 0 / 0 |
| MH_05 status | completed_with_warnings | completed_with_warnings |
| MH_05 wall_s / rtf | 535.7 / 0.212 | 272.6 / 0.417 |

probe-b 时间线（i=425…429）：

| frame i | 备注 | zombie | shared（④f） |
|---:|---|---:|---:|
| 425 | `drops_skipped`；culled_ids=32 | 0 | 45 |
| 426 | deferred flush before process；culled=1 | **0** | 13 |
| 427 | — | **0** | **29**（④f=16） |
| 428 | — | **0** | **43**（④f=29） |
| 429 | — | **0** | **52**（④f=34） |

解读：下帧对 32-id 集精确 `dropTracks` **清除 zombie**（机制目标达成），但
复现 ④e「大剔后 drop」对 ATE 的伤害（4.565）；④f「skip 且永不 drop 该集」在
ATE 上仍更好（3.057）。shared 427–429 **未收窄**，反而更宽。

### 12.4 全序列表（Slice ④g）

**未跑。** MH_05 软门不够 → 按设计停止扩 scope；不跑其余 10 条。

### 12.5 判定与后续（门控当时）

- **④g 门控不够 / 部分完成**：编码与契约落地；MH_01 硬门不回归；zombie 归零；
  MH_05 ATE **不够**（回 ④e 平台）。
- **禁止**在未达软改善前跑全序列；**不开** ⑤。
- 下一刀曾标 **TBD**；默认编排回退见 §12.6。

### 12.6 默认编排回退（事后诊断后）

事后对拍（[postmortem](m3.3-slice4g-postmortem.md)）：④g 的 `diag.csv` /
`est.tum` 与 ④e **bit-identical**——相对下一帧 `process`，一帧延后 ≡ 立即
drop。伤 ATE 的是 drop 该 32-id 集本身，不是「延得不够晚」。

**已决**：删除 `pending_drop` 编排，默认恢复 ④f「skip 则不 drop」；保留
`deferred_drops` / `deferred_drop_ids` 合同（默认恒 0）。④g 记为「已证伪作
ATE 改善刀」。下一刀方向仍 **TBD**（禁止再延后一帧同批 drop / 默认开 ⑤）。

回退验收（commit `ba2983d` / `default_a5e90dc7`；scratch `/tmp/mh01_revert`、
`/tmp/mh05_revert`）：

| 门 | 实测 | 结果 |
|---|---|---|
| MH_01 ATE / skip / deferred | 0.0987839 / 0 / 0 | **PASS** |
| MH_05 ATE / RPE / skip / deferred | **3.056878** / 0.772423 / 1 / 0 | **PASS**（=④f） |
| MH_05 `est.tum` / `diag.csv` vs ④f `c446ac5` | 逐字节相同 | **PASS** |
| probe-b i=426 zombie | 32（④f 形态） | OK |
